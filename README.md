# Calci-cpp
Console-based C++ calculator with password-protected access. Supports addition, subtraction, multiplication, division, and string concatenation using templates. Includes system settings to change password and uses file I/O for persistent storage. Menu-driven interface with input validation.
